<h4>Add tasklist</h4>
<%= form_for([@project, @project.tasklists.build]) do |f| %>
<p>
<%= f.label :name %> <br>
<%= f.text_field(:name, :class => 'form-control') %>
</p>
<p>
 <%= f.button :submit, :class => 'btn btn-default' %>
</p>
<% end %>